import { Typography } from '@material-ui/core';
import React from 'react';

const Exchange = () => {
  return (
    <div>
      <Typography>This is Exchange page</Typography>
    </div>
  );
};

export default Exchange;
